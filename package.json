{
  "name": "siptracker",
  "displayName": "Sip Tracker",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.120.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./extension.js",
  "files": [
    "extension.js",
    "webview.html",
    "webview.css"
  ],
  "contributes": {
    "commands": [
      {
        "command": "siptracker.stop",
        "title": "Sip Tracker: Stop Timer"
      },
      {
        "command": "siptracker.reset",
        "title": "Sip Tracker: Reset Timer"
      },
      {
        "command": "siptracker.start",
        "title": "Sip Tracker: Start Timer"
      },
      {
        "command": "siptracker.showStats",
        "title": "Sip Tracker: Show Stats"
      },
      {
        "command": "siptracker.showAchievements",
        "title": "Sip Tracker: Show Achievements"
      }
    ],
    "configuration": {
      "title": "siptracker",
      "properties": {
        "siptracker.sipInterval": {
          "type": "number",
          "default": 0.25,
          "min": 0,
          "description": "How often to remind you to drink water (in minutes)"
        },
        "siptracker.sipMsg": {
          "type": "string",
          "default": "Time For a Sip!!",
          "description": "Set your own reminder message"
        }
      }
    }
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.120.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "eslint": "^9.39.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}